Analyze {{SYMBOL}} using only the data below. Today is {{TODAY}}.

Every figure carries `source` and `asOf`. `value: null` means unverified. Currency is the company's reporting currency shown in `currency`; do not convert.

```json
{{STOCK_DATA_JSON}}
```

{{PREVIOUS_TRIPWIRE}}

Return the JSON object described in the output contract.
